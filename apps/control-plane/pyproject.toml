[project]
name = "airmux-control-plane"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
  "airmux-contract",
  "airmux-runtime",
  "fastapi",
  "sqlmodel",
  "sqlalchemy[asyncio]>=2",
  "asyncpg",
  "alembic",
  "uvicorn[standard]",
  "pyyaml",
  "argon2-cffi>=23.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/control_plane"]
