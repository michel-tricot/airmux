[project]
name = "gw-cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "gw-contract",
  "httpx",
]

[project.scripts]
gw = "gw_cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gw_cli"]
