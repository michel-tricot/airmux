[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "contract",
  "httpx",
]

[project.scripts]
airllm = "cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli"]
