[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "contract",
  "httpx",
  "typer",
  "python-dotenv",
  "pyyaml",
  "rich",
  "tomli-w>=1.2.0",
]

[project.scripts]
airllm = "cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli"]
