[project]
name = "airmux"
version = "0.1.1"
requires-python = ">=3.13"
dependencies = [
  "airmux-api-models==0.1.1",
  "airmux-contract==0.1.1",
  "airmux-data-plane==0.1.1",
  "airmux-control-plane==0.1.1",
  "httpx",
  "typer",
  "python-dotenv",
  "pyyaml",
  "rich",
  "tomli-w>=1.2.0",
]

[project.scripts]
airmux = "cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli"]
