name: deploy-fly

on:
  workflow_dispatch:

concurrency:
  group: deploy-fly-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    env:
      AIRMUX_PUBLIC_URL: ${{ vars.AIRMUX_PUBLIC_URL }}
      FLY_APP: ${{ vars.FLY_APP }}
    steps:
      - uses: actions/checkout@v7
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: >-
          flyctl deploy --config deploy/fly/fly.toml --app "$FLY_APP"
          --remote-only --ha=false
          --env "AIRMUX_CONSOLE_URL=${AIRMUX_PUBLIC_URL:-https://$FLY_APP.fly.dev}"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      - run: AIRMUX_PUBLIC_URL="${AIRMUX_PUBLIC_URL:-https://$FLY_APP.fly.dev}" ./deploy/smoke.sh
