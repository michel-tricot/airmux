# Generated by `airllmdp schema`; never hand-edit.
$defs:
  Adjustment:
    additionalProperties: false
    description: One reconciliation the gateway made to a request, reported on the response rather than silent.
    properties:
      param:
        title: Param
        type: string
      action:
        enum:
        - clamped
        - emulated
        - dropped
        title: Action
        type: string
      detail:
        title: Detail
        type: string
    required:
    - param
    - action
    - detail
    title: Adjustment
    type: object
  GatewayInfo:
    additionalProperties: false
    description: 'What the gateway did on the way to the provider: the one namespaced place data plane

      internals surface to the caller, so the core response stays about the completion. Grows

      additively as the router grows.'
    properties:
      adjustments:
        items:
          $ref: '#/$defs/Adjustment'
        title: Adjustments
        type: array
    title: GatewayInfo
    type: object
  ReasoningDelta:
    additionalProperties: false
    description: signature arrives on the closing fragment of a reasoning block, text on the ones before it.
    properties:
      type:
        const: reasoning
        default: reasoning
        title: Type
        type: string
      text:
        default: ''
        title: Text
        type: string
      signature:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Signature
    title: ReasoningDelta
    type: object
  TextDelta:
    additionalProperties: false
    properties:
      type:
        const: text
        default: text
        title: Type
        type: string
      text:
        title: Text
        type: string
    required:
    - text
    title: TextDelta
    type: object
  ToolCallDelta:
    additionalProperties: false
    description: 'One fragment of a tool call. index ties fragments of parallel calls together: id and name

      arrive on the first fragment, arguments accrete as JSON text across the rest.'
    properties:
      type:
        const: tool_call
        default: tool_call
        title: Type
        type: string
      index:
        minimum: 0
        title: Index
        type: integer
      id:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Id
      name:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Name
      arguments:
        default: ''
        title: Arguments
        type: string
    required:
    - index
    title: ToolCallDelta
    type: object
  Usage:
    additionalProperties: false
    properties:
      input_tokens:
        default: 0
        title: Input Tokens
        type: integer
      output_tokens:
        default: 0
        title: Output Tokens
        type: integer
      cache_read_tokens:
        default: 0
        title: Cache Read Tokens
        type: integer
      cache_write_tokens:
        default: 0
        title: Cache Write Tokens
        type: integer
      estimated:
        default: false
        title: Estimated
        type: boolean
    title: Usage
    type: object
additionalProperties: false
description: One streamed increment. The closing chunk carries finish_reason, usage and gateway, and no delta.
properties:
  id:
    title: Id
    type: string
  delta:
    anyOf:
    - discriminator:
        mapping:
          reasoning: '#/$defs/ReasoningDelta'
          text: '#/$defs/TextDelta'
          tool_call: '#/$defs/ToolCallDelta'
        propertyName: type
      oneOf:
      - $ref: '#/$defs/TextDelta'
      - $ref: '#/$defs/ReasoningDelta'
      - $ref: '#/$defs/ToolCallDelta'
    - type: 'null'
    default: null
    title: Delta
  finish_reason:
    anyOf:
    - enum:
      - stop
      - length
      - tool_calls
      - content_filter
      type: string
    - type: 'null'
    default: null
    title: Finish Reason
  usage:
    anyOf:
    - $ref: '#/$defs/Usage'
    - type: 'null'
    default: null
  gateway:
    anyOf:
    - $ref: '#/$defs/GatewayInfo'
    - type: 'null'
    default: null
required:
- id
title: CanonicalChunk
type: object
