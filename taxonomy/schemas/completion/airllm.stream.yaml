# Generated by `airllmdp schema`; never hand-edit.
$defs:
  Adjustment:
    additionalProperties: false
    description: One reconciliation the gateway made to a request, reported on the response rather than silent.
    properties:
      param:
        title: Param
        type: string
      action:
        enum:
        - clamped
        - emulated
        - dropped
        title: Action
        type: string
      detail:
        title: Detail
        type: string
    required:
    - param
    - action
    - detail
    title: Adjustment
    type: object
  DeltaChunk:
    additionalProperties: false
    properties:
      id:
        title: Id
        type: string
      delta:
        discriminator:
          mapping:
            reasoning: '#/$defs/ReasoningDelta'
            text: '#/$defs/TextDelta'
            tool_call: '#/$defs/ToolCallDelta'
          propertyName: type
        oneOf:
        - $ref: '#/$defs/TextDelta'
        - $ref: '#/$defs/ReasoningDelta'
        - $ref: '#/$defs/ToolCallDelta'
        title: Delta
      finish_reason:
        default: null
        title: Finish Reason
        type: 'null'
      usage:
        default: null
        title: Usage
        type: 'null'
      gateway:
        default: null
        title: Gateway
        type: 'null'
    required:
    - id
    - delta
    title: DeltaChunk
    type: object
  FinalChunk:
    additionalProperties: false
    properties:
      id:
        title: Id
        type: string
      delta:
        default: null
        title: Delta
        type: 'null'
      finish_reason:
        anyOf:
        - enum:
          - stop
          - length
          - tool_calls
          - content_filter
          type: string
        - type: 'null'
        default: null
        title: Finish Reason
      usage:
        $ref: '#/$defs/Usage'
      gateway:
        anyOf:
        - $ref: '#/$defs/GatewayInfo'
        - type: 'null'
        default: null
    required:
    - id
    - usage
    title: FinalChunk
    type: object
  GatewayInfo:
    additionalProperties: false
    description: 'What the gateway did on the way to the provider: the one namespaced place data plane

      internals surface to the caller, so the core response stays about the completion. Grows

      additively as the router grows.'
    properties:
      finish_reason:
        anyOf:
        - enum:
          - stop
          - length
          - tool_calls
          - content_filter
          type: string
        - type: 'null'
        default: null
        title: Finish Reason
      adjustments:
        items:
          $ref: '#/$defs/Adjustment'
        title: Adjustments
        type: array
    title: GatewayInfo
    type: object
  ReasoningDelta:
    additionalProperties: false
    description: The provider item id opens replayable reasoning, text follows, and the signature may arrive on a later fragment.
    properties:
      type:
        const: reasoning
        default: reasoning
        title: Type
        type: string
      id:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Id
      text:
        default: ''
        title: Text
        type: string
      signature:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Signature
    title: ReasoningDelta
    type: object
  TextDelta:
    additionalProperties: false
    properties:
      type:
        const: text
        default: text
        title: Type
        type: string
      text:
        title: Text
        type: string
    required:
    - text
    title: TextDelta
    type: object
  ToolCallDelta:
    additionalProperties: false
    description: 'One fragment of a tool call. index ties fragments of parallel calls together: id and name

      arrive on the first fragment, arguments accrete as JSON text across the rest.'
    properties:
      type:
        const: tool_call
        default: tool_call
        title: Type
        type: string
      index:
        minimum: 0
        title: Index
        type: integer
      id:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Id
      name:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Name
      arguments:
        default: ''
        title: Arguments
        type: string
    required:
    - index
    title: ToolCallDelta
    type: object
  Usage:
    additionalProperties: false
    properties:
      input_tokens:
        default: 0
        title: Input Tokens
        type: integer
      output_tokens:
        default: 0
        title: Output Tokens
        type: integer
      cache_read_tokens:
        default: 0
        title: Cache Read Tokens
        type: integer
      cache_write_tokens:
        default: 0
        title: Cache Write Tokens
        type: integer
      estimated:
        default: false
        title: Estimated
        type: boolean
    title: Usage
    type: object
anyOf:
- $ref: '#/$defs/DeltaChunk'
- $ref: '#/$defs/FinalChunk'
