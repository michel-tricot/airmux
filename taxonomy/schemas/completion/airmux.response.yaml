# Generated by `airmux gateway schema`; never hand-edit.
$defs:
  CanonicalAdjustment:
    additionalProperties: false
    description: One reconciliation the gateway made to a request, reported on the response rather than silent.
    properties:
      param:
        title: Param
        type: string
      action:
        enum:
        - clamped
        - emulated
        - dropped
        title: Action
        type: string
      detail:
        title: Detail
        type: string
    required:
    - param
    - action
    - detail
    title: CanonicalAdjustment
    type: object
  CanonicalGatewayInfo:
    additionalProperties: false
    description: 'What the gateway did on the way to the provider: the one namespaced place data plane

      internals surface to the caller, so the core response stays about the completion. Grows

      additively as the router grows.'
    properties:
      finish_reason:
        anyOf:
        - enum:
          - stop
          - length
          - tool_calls
          - content_filter
          type: string
        - type: 'null'
        default: null
        title: Finish Reason
      adjustments:
        items:
          $ref: '#/$defs/CanonicalAdjustment'
        title: Adjustments
        type: array
    title: CanonicalGatewayInfo
    type: object
  CanonicalReasoningPart:
    additionalProperties: false
    description: 'Model reasoning. signature is an opaque provider token: a provider that issues one rejects a

      later turn whose reasoning comes back without it.'
    properties:
      cache:
        anyOf:
        - const: ephemeral
          type: string
        - type: 'null'
        default: null
        title: Cache
      type:
        const: reasoning
        default: reasoning
        title: Type
        type: string
      id:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Id
      text:
        title: Text
        type: string
      signature:
        anyOf:
        - type: string
        - type: 'null'
        default: null
        title: Signature
    required:
    - text
    title: CanonicalReasoningPart
    type: object
  CanonicalTextPart:
    additionalProperties: false
    properties:
      cache:
        anyOf:
        - const: ephemeral
          type: string
        - type: 'null'
        default: null
        title: Cache
      type:
        const: text
        default: text
        title: Type
        type: string
      text:
        title: Text
        type: string
    required:
    - text
    title: CanonicalTextPart
    type: object
  CanonicalToolCallPart:
    additionalProperties: false
    description: 'A tool invocation. arguments stays JSON text: parsing it loses a truncated stream''s partial

      arguments and re-serializing changes the key order the provider chose.'
    properties:
      cache:
        anyOf:
        - const: ephemeral
          type: string
        - type: 'null'
        default: null
        title: Cache
      type:
        const: tool_call
        default: tool_call
        title: Type
        type: string
      id:
        title: Id
        type: string
      name:
        title: Name
        type: string
      arguments:
        title: Arguments
        type: string
    required:
    - id
    - name
    - arguments
    title: CanonicalToolCallPart
    type: object
  CanonicalUsage:
    additionalProperties: false
    properties:
      input_tokens:
        default: 0
        title: Input Tokens
        type: integer
      output_tokens:
        default: 0
        title: Output Tokens
        type: integer
      cache_read_tokens:
        default: 0
        title: Cache Read Tokens
        type: integer
      cache_write_tokens:
        default: 0
        title: Cache Write Tokens
        type: integer
      estimated:
        default: false
        title: Estimated
        type: boolean
    title: CanonicalUsage
    type: object
additionalProperties: false
properties:
  id:
    title: Id
    type: string
  model:
    title: Model
    type: string
  content:
    items:
      discriminator:
        mapping:
          reasoning: '#/$defs/CanonicalReasoningPart'
          text: '#/$defs/CanonicalTextPart'
          tool_call: '#/$defs/CanonicalToolCallPart'
        propertyName: type
      oneOf:
      - $ref: '#/$defs/CanonicalTextPart'
      - $ref: '#/$defs/CanonicalReasoningPart'
      - $ref: '#/$defs/CanonicalToolCallPart'
    title: Content
    type: array
  finish_reason:
    anyOf:
    - enum:
      - stop
      - length
      - tool_calls
      - content_filter
      type: string
    - type: 'null'
    title: Finish Reason
  usage:
    $ref: '#/$defs/CanonicalUsage'
  gateway:
    $ref: '#/$defs/CanonicalGatewayInfo'
required:
- id
- model
- content
- finish_reason
- usage
title: CanonicalResponse
type: object
