[project]
name = "airmux-contract"
version = "0.1.1"
requires-python = ">=3.13"
dependencies = [
  "asyncpg",
  "pydantic>=2",
  "pyyaml",
  "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contract"]
