[project]
name = "airmux-api-models"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
  "pydantic>=2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api_models"]
