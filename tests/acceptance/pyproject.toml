[project]
name = "acceptance"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "httpx",
  "playwright",
  "pyyaml",
  "python-dotenv",
  "openai",
  "testcontainers",
]

[tool.uv]
package = false

[tool.ruff]
extend = "../../pyproject.toml"
